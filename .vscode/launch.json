{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "current file",
            "type": "debugpy",
            "request": "launch",
            "justMyCode": false,
            "cwd": "${workspaceFolder}",
            "program": "${file}"
        }
    ]
}